package jp.co.bitz.gameplaykit

import kotlin.test.Test
import kotlin.test.assertTrue

// Smoke test confirming the unit test toolchain runs; replace as real specs land in Phase 1.
class ProjectSetupTest {
    @Test
    fun `unit test infrastructure is wired up`() {
        assertTrue(true)
    }
}
